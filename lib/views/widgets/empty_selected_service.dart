import 'package:flutter/material.dart';

class EmptySelectedService extends StatelessWidget {
  const EmptySelectedService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Belum ada layanan dipilih'));
  }
}
